<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <link rel="icon" href="https://cdn-icons-png.flaticon.com/512/653/653021.png" type="image/png">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <!-- estilos centralizados en /css/style.css -->
</head>

<body>

  <!-- ============================================
       Sidebar (componente parcial)
       - Contiene el menú de navegación lateral
       ============================================ -->
  <%- include('partials/sidebar') %>

  <!-- ============================================
       Header fijo (componente parcial)
       - Muestra el título de la sección principal
       ============================================ -->
  <%- include('partials/header', { headerTitle: 'Noticias noticiosamente noticiosas' }) %>

  <!-- ============================================
       Contenedor principal del contenido dinámico
       ============================================ -->
  <div class="main-content">

    <!-- ======================================================
         BLOQUE: Buscador de noticias
         - Permite filtrar resultados por título
         - Envia los parámetros al endpoint "/search" (GET)
         ====================================================== -->
    <form id="searchForm" action="/search" method="get" class="mb-4">
      <div class="input-group">
        <input 
          type="text" 
          name="term" 
          class="form-control" 
          placeholder="Buscar por título..." 
          value="<%= search || '' %>">
        <button class="btn btn-primary" type="submit">Buscar</button>
      </div>
    </form>

    <!-- ======================================================
         BLOQUE: Listado de noticias
         - Recorre el array "news" e imprime cada noticia
         - Si no hay noticias, muestra un mensaje informativo
         ====================================================== -->
    <div class="row g-4">
      <% if (news.length === 0) { %>
        <!-- Caso: no hay noticias -->
        <p class="text-center text-muted">No hay noticias disponibles.</p>

      <% } else { %>
        <!-- Caso: existen noticias -->
        <% news.forEach(n => { %>
          <div class="col-md-4">
            <div class="card news-card shadow-sm">
              <!-- Imagen principal de la noticia -->
              <img src="<%= n.image %>" alt="<%= n.title %>">

              <div class="card-body">
                <!-- Título de la noticia -->
                <h5 class="card-title"><%= n.title %></h5>

                <!-- Resumen breve -->
                <p class="card-text"><%= n.summary %></p>

                <!-- Fecha de publicación -->
                <p class="text-secondary small mt-auto"><%= n.date %></p>

                <!-- Enlace al detalle de la noticia -->
                <a href="/news/<%= n.id %>" class="btn btn-outline-primary btn-sm mt-2">Leer más</a>
              </div>
            </div>
          </div>
        <% }) %>
      <% } %>
    </div>

    <!-- ======================================================
         BLOQUE: Paginación
         - Se muestra solo si hay más de una página de resultados
         - Usa parámetros "term" y "page" para conservar filtros
         ====================================================== -->
    <% if (totalPages > 1) { %>
      <nav aria-label="Paginación" class="mt-5">
        <ul class="pagination justify-content-center">

          <!-- Botón: página anterior -->
          <li class="page-item <%= page <= 1 ? 'disabled' : '' %>">
            <a class="page-link"
              href="?term=<%= search || '' %>&page=<%= page - 1 %>">Anterior</a>
          </li>

          <!-- Botones numéricos -->
          <% for (let i = 1; i <= totalPages; i++) { %>
            <li class="page-item <%= page === i ? 'active' : '' %>">
              <a class="page-link"
                href="?term=<%= search || '' %>&page=<%= i %>"><%= i %></a>
            </li>
          <% } %>

          <!-- Botón: página siguiente -->
          <li class="page-item <%= page >= totalPages ? 'disabled' : '' %>">
            <a class="page-link"
              href="?term=<%= search || '' %>&page=<%= page + 1 %>">Siguiente</a>
          </li>

        </ul>
      </nav>
    <% } %>

  </div>

  <!-- ============================================
       Footer (componente parcial)
       - Incluye pie de página general del sitio
       ============================================ -->
  <%- include('partials/footer') %>

</body>
</html>