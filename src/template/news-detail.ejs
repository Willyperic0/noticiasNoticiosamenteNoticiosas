<!DOCTYPE html> <!-- Define el tipo de documento HTML5 -->
<html lang="es"> <!-- Idioma del documento establecido en español -->
<head>
  <meta charset="UTF-8" /> <!-- Codificación de caracteres para soporte UTF-8 -->
  <meta name="viewport" content="width=device-width, initial-scale=1.0"> <!-- Hace que el sitio sea responsive en dispositivos móviles -->
  <title><%= article.title %></title> <!-- Título dinámico obtenido desde el objeto "article" pasado al render EJS -->
  <link rel="icon" href="https://cdn-icons-png.flaticon.com/512/653/653021.png" type="image/png"> <!-- Favicon del sitio -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"> <!-- Inclusión de Bootstrap v5.3.3 desde CDN -->
  <link rel="stylesheet" href="/css/style.css"> <!-- Enlace al archivo CSS principal del proyecto -->
</head>
<body>
  <!-- =========================================================
       Componentes parciales (EJS Includes)
       ========================================================= -->
  <%- include('partials/sidebar') %> <!-- Barra lateral -->
  <%- include('partials/header', { headerTitle: 'Noticia' }) %> <!-- Encabezado con título estático "Noticia" -->

  <!-- =========================================================
       Contenido principal de la página
       ========================================================= -->
  <div class="main-content">
    <article class="blog-article"> <!-- Artículo individual de tipo "noticia" -->

      <!-- =======================
           Título y metadatos
           ======================= -->
      <h2><%= article.title %></h2> <!-- Título principal del artículo -->
      <p class="article-date text-muted"><%= article.date %></p> <!-- Fecha de publicación (texto atenuado) -->
      
      <!-- =======================
           Imagen principal (condicional)
           - Se renderiza solo si "article.image" existe
           ======================= -->
      <% if (article.image) { %>
        <img src="<%= article.image %>" alt="<%= article.title %>">
      <% } %>

      <!-- =======================
           Resumen introductorio
           ======================= -->
      <p class="lead"><%= article.summary %></p> <!-- Texto destacado tipo "lead" -->

      <!-- =======================
           Cuerpo completo del artículo
           ======================= -->
      <div class="article-body"><%= article.content %></div> <!-- Contenido principal dinámico -->
    </article>
  </div>

  <!-- =========================================================
       Pie de página (partial)
       ========================================================= -->
  <%- include('partials/footer') %> <!-- Inserta footer común del sitio -->
</body>
</html>