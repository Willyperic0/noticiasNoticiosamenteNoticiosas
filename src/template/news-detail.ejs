<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title><%= article.title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { background: #fafafa; font-family: 'Segoe UI', sans-serif; }
    header { background: #222; color: white; padding: 1rem 2rem; display: flex; justify-content: space-between; align-items: center; }
    header a { color: #ddd; text-decoration: none; }
    header a:hover { color: white; }
    article { max-width: 800px; margin: 2rem auto; background: white; padding: 2rem; border-radius: 8px; box-shadow: 0 2px 8px rgba(0,0,0,0.1); }
    article img { width: 100%; border-radius: 6px; margin-bottom: 1rem; object-fit: cover; }
    footer { margin-top: 3rem; text-align: center; color: #888; padding: 1rem; font-size: 0.9rem; }
  </style>
</head>
<body>
  <header>
    <a href="/news">← Volver</a>
    <h1>📰 Noticia</h1>
    <span></span>
  </header>

  <article>
    <h2><%= article.title %></h2>
    <p class="text-muted">📅 <%= article.date %></p>
    <img src="<%= article.image %>" alt="<%= article.title %>">
    <p class="lead"><%= article.summary %></p>
    <p><%= article.content %></p>
  </article>

  <footer>© <%= new Date().getFullYear() %> Tablón de Noticias</footer>
</body>
</html>
